---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: governance-constraint-templates
  namespace: argocd
  labels:
    app.kubernetes.io/name: governance-templates
    layer: security
  annotations:
    argocd.argoproj.io/sync-wave: "1"  # Após Gatekeeper
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  
  source:
    repoURL: https://github.com/samuelBarreto/governance.git  # ALTERAR
    targetRevision: main
    path: policies
    directory:
      recurse: true
      include: '*-template.yaml'  # Apenas ConstraintTemplates
  
  destination:
    server: https://kubernetes.default.svc
    namespace: gatekeeper-system
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - RespectIgnoreDifferences=true
    retry:
      limit: 10
      backoff:
        duration: 20s
        factor: 2
        maxDuration: 10m
  
  # Ignorar diferenças em status
  ignoreDifferences:
  - group: templates.gatekeeper.sh
    kind: ConstraintTemplate
    jsonPointers:
    - /status

