---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bedrock-agent
  namespace: argocd
  labels:
    app.kubernetes.io/name: bedrock-agent
    layer: workloads
    environment: dev
  annotations:
    argocd.argoproj.io/sync-wave: "8"  # Deploy após hml
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  
  source:
    repoURL: https://github.com/samuelBarreto/argocd.git  # ALTERAR para seu repo principal
    targetRevision: main  # ou HEAD para sempre pegar a branch atual
    path: bedrock-agent
    # Kustomize automaticamente detectado (kustomization.yaml presente)
  
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  
  syncPolicy:
    automated:
      prune: true       # Remove recursos deletados do Git
      selfHeal: true    # Corrige drift automaticamente
    syncOptions:
    - CreateNamespace=true
    - ApplyOutOfSyncOnly=true  # Aplica apenas o que mudou
    - SkipDryRunOnMissingResource=true  # Aguardar XRDs estarem prontos
    retry:
      limit: 10
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 10m
  
  # Ignorar diferenças em status (gerenciado pelo Crossplane)
  ignoreDifferences:
  - group: iam.aws.upbound.io
    kind: Role
    jsonPointers:
    - /status
  - group: iam.aws.upbound.io
    kind: RolePolicy
    jsonPointers:
    - /status
